# SQLCheck 2.0 系統架構與設計簡易說明

> 這份文件是給非技術人員閱讀的簡易版。  
> 目的不是說明程式怎麼寫，而是讓你可以快速理解：**這套系統要做什麼、怎麼運作、每個元件負責什麼，以及為什麼我們要這樣設計。**

---

# 一、先用一句話理解 SQLCheck 2.0

SQLCheck 2.0 是一個：

> **「先依規則檢查 SQL，再由 AI 用白話提供改善建議」的內部輔助工具。**

它不是用來直接執行 SQL，也不是拿來連財政資訊中心資料庫。

同仁只需要輸入：

1. **申請單號**
2. **SQL**
3. **COST 值**

然後系統會幫忙：

- 檢查是否符合既有規範。
- 找出值得改善的地方。
- 計算「改善優先指數」。
- 用臺灣繁體中文說明改善方向。
- 必要時提供一份「建議寫法」。
- 由 AI 預估大約可以改善多少效能。

---

# 二、整個系統的操作流程

可以把它想成下面這條流程：

```text
同仁輸入申請單號、SQL、COST
             ↓
      系統先做規則檢核
             ↓
      找出可改善的地方
             ↓
     算出改善優先指數
             ↓
     交給 AI 做白話說明
             ↓
  AI 提供改善建議與建議寫法
             ↓
  AI 預估可能改善的效能比例
             ↓
       網頁用圖卡顯示結果
             ↓
     使用者可列印／存成 PDF
```

整個流程只有一次輸入，不會要求同仁再回頭補第二次 COST。

---

# 三、這套系統「不做」什麼

這一點非常重要。

SQLCheck 2.0 **不會**：

- 連財政資訊中心 Oracle 資料庫。
- 讀取正式資料庫資料。
- 自動執行 SQL。
- 自動修改正式資料。
- 自動新增或修改 Index。
- 自動計算 Oracle 真正的改善後 COST。
- 自動判定 AI 建議一定正確。
- 保存歷史案件。
- 建立自己的資料庫。

所以它的定位很單純：

> **只做「事前檢核 + 改善建議」。**

---

# 四、系統實際跑在哪裡？

整套系統跑在一台 Windows 11 工作站上。

這台電腦有：

- Windows 11
- NVIDIA RTX 4090 24GB
- Ollama
- Gemma 4 31B
- Docker Desktop

其中：

```text
Windows 11
│
├─ Ollama
│   └─ Gemma 4 31B
│      使用 RTX 4090 運算
│
└─ Docker Desktop
    └─ 只有 1 個 SQLCheck Container
```

也就是說：

> **只有 SQLCheck 本身放在 Docker 裡。**

AI 模型並不放進 Docker，而是直接由 Windows 上的 Ollama 執行。

這樣的好處是：

- 架構簡單。
- 模型更新與程式更新互不影響。
- 不需要維護很多 Container。
- RTX 4090 可以直接給 Ollama 使用。

---

# 五、為什麼還是會有「前端」跟「後端」？

雖然系統只有一個 Container，但裡面仍會分成兩個部分。

## 前端

就是同仁看到的網頁。

負責：

- 輸入申請單號。
- 輸入 COST。
- 貼上 SQL。
- 上傳附件。
- 顯示檢核結果。
- 顯示改善建議。
- 顯示預估改善比例。
- 顯示原始 SQL 與建議寫法。
- 列印／存成 PDF。

前端主要使用：

> React + TypeScript + Vite

但這些技術名稱對一般使用者其實不用知道。

可以簡單理解成：

> **前端就是畫面。**

---

## 後端

後端是看不到的處理核心。

負責：

- 讀取 SQL。
- 解析附件。
- 判斷 SQL 結構。
- 執行規則檢核。
- 計算改善優先指數。
- 把整理後的資訊交給 AI。
- 接收 AI 回覆。
- 把結果整理給前端。

可以簡單理解成：

> **後端就是整套系統真正做判斷與控制的地方。**

---

# 六、為什麼不用資料庫？

這套 SQLCheck 不需要保存案件。

同仁完成一次檢核後：

- SQL 不會長期保存。
- COST 不會長期保存。
- AI 建議不會長期保存。
- 上傳附件不會長期保存。

資料只存在這一次操作過程。

完成後，同仁可以：

> **列印或另存 PDF，再依原本的 SPM 電子簽核流程保存。**

所以我們沒有必要再建立：

- SQLite
- PostgreSQL
- MySQL
- 其他案件資料庫

這樣可以少掉：

- 資料庫備份。
- 資料庫維護。
- Schema 升級。
- 歷史資料清理。
- 額外資安管理。

系統會更單純。

---

# 七、同仁需要輸入什麼？

只有三項主要資料。

## 1. 申請單號

例如：

```text
115000123
```

只是用來識別這一次檢核與列印報告。

---

## 2. SQL

同仁可以直接貼上：

```sql
SELECT ...
FROM ...
WHERE ...
```

如果 SQL 太長，也可以用附件上傳。

---

## 3. COST

例如：

```text
68420
```

這個 COST 是同仁從原本既有工具取得的數字。

SQLCheck 不會自己連 Oracle 去算 COST。

---

# 八、SQL 太長怎麼辦？

可以直接上傳附件。

第一版預計支援：

- `.docx`
- `.pdf`
- `.txt`
- `.csv`
- `.md`
- `.sql`

系統會：

```text
讀取附件
   ↓
找出裡面的文字
   ↓
判斷哪些內容像 SQL
   ↓
把 SQL 放回畫面
   ↓
讓使用者先確認
   ↓
再開始檢核
```

這裡有一個重要原則：

> **附件上傳只是幫忙把 SQL 抓出來，不是文件管理功能。**

系統不會把附件保存起來。

---

# 九、Word、PDF 真的都能讀嗎？

可以，但有限制。

## Word

`.docx` 可以讀：

- 一般段落。
- 表格裡面的文字。

系統會從裡面找 SQL。

---

## PDF

第一版只處理：

> **可以反白選取文字的 PDF。**

如果是掃描紙本形成的 PDF，沒有真正的文字層，就不做 OCR。

畫面會直接提醒：

> 此 PDF 無法擷取文字，請改用可選取文字的 PDF、DOCX、TXT，或直接貼上 SQL。

這樣比較安全，也避免 OCR 把 SQL 的括號、單引號或欄位名稱看錯。

---

# 十、誰負責判定「符合不符合」？

這裡要特別分清楚。

系統裡有兩個不同角色：

## 角色一：規則引擎

它負責「明確的規則」。

例如：

- COST 是否超過 100,000。
- 是否有 WHERE。
- 是否使用 Parallel Hint。
- 是否出現 OR。
- 是否使用 `TRUNC()`。
- 是否查詢指定重要資料表。

這些是由程式判定。

> **不是由 AI 猜。**

---

## 角色二：AI

AI 負責：

- 把結果講得比較容易懂。
- 提供改善方向。
- 必要時提供一份建議寫法。
- 預估可能的效能改善比例。

所以可以把它理解成：

> **規則引擎是裁判，AI 是顧問。**

AI 不能推翻規則引擎的判定。

---

# 十一、什麼是「中心規範」？

畫面會先告訴同仁：

> **符合中心規範**

或：

> **不符合中心規範**

例如：

```text
中心規範
符合
另有 2 項提醒
```

這裡的「提醒」不代表違規。

例如：

- 使用 `TRUNC()`。
- 使用 OR。
- 查詢 HOUT120。
- 某些條件可能值得再調整。

這些可以只是：

> **改善提醒。**

所以「符合規範」跟「還可以改善」可以同時成立。

---

# 十二、什麼是「改善優先指數」？

我們不要使用「風險分數」這種容易讓同仁有壓力的名稱。

所以正式名稱是：

> **改善優先指數**

它的意思是：

> **這支 SQL 有多少地方值得優先檢視或改善。**

不是違規分數，也不是 SQL 成績。

目前定義：

| 分數 | 顯示 | 顏色 |
|---:|---|---|
| 0–59 | 目前良好 | 綠色 |
| 60–79 | 建議改善 | 黃色 |
| 80–100 | 優先改善 | 紅色 |

例如：

```text
改善優先指數
68 / 100
建議改善
```

它只是告訴同仁：

> 「這支 SQL 有幾個地方值得先看一下。」

---

# 十三、誰來算這個分數？

不是 AI。

改善優先指數由 SQLCheck 自己的規則引擎計算。

例如未來可以設定：

```text
某種條件出現
+ 某個分數

另一種條件出現
+ 某個分數
```

最後最多 100 分。

具體每一項加多少分，之後可以再依實際需求調整。

這樣做的好處是：

> **同一支 SQL 每次檢查，分數邏輯都是一致的。**

不會因為 AI 每次回答不同而忽高忽低。

---

# 十四、AI 實際會做什麼？

目前 AI 使用：

> **Gemma 4 31B**

透過 Windows 上的 Ollama 執行。

它主要做四件事。

## 1. 白話解釋

例如系統看到：

```sql
TRUNC(TXN_DATE)
```

AI 不會講一大堆 Oracle 理論。

而是用簡單方式說：

> 日期條件目前使用 `TRUNC()`，可以評估改成日期範圍寫法。

---

## 2. 提供改善建議

例如：

```sql
TXN_DATE >= :起始日
AND TXN_DATE < :隔日
```

讓同仁知道可以往哪個方向調整。

---

## 3. 提供建議寫法

若 SQL 結構比較單純，而且系統允許，AI 可以另外提供：

> **建議寫法**

但原始 SQL 永遠保留。

系統不會直接把原始 SQL 蓋掉。

---

## 4. 預估效能改善幅度

例如：

```text
45%
```

意思是：

> AI 判斷依照目前的改善方向，可能約有 45% 的效能改善潛力。

這只是參考，不是 Oracle 真正實測值。

---

# 十五、為什麼 AI 可以估改善比例，卻不能算新的 COST？

因為兩件事情不同。

AI 可以根據：

- SQL 結構。
- 原始 COST。
- 發現的改善項目。
- 建議改寫內容。

做一個：

> **大概有多少改善潛力的估計。**

例如：

```text
預估改善效果
45%
```

但是 AI 不知道：

- 真實資料量。
- Oracle 統計資訊。
- 實際 Index。
- Oracle Optimizer 最後會選哪條路。

所以不能假裝知道：

```text
改善後 COST = 19,160
```

這種數字。

因此畫面只顯示：

> **預估改善比例**

不顯示假想的改善後 COST。

---

# 十六、預估改善效果怎麼呈現？

畫面會用很簡單的視覺方式。

例如：

```text
       45%

AI 預估效能改善幅度
```

搭配：

- 圓形進度環。
- 半圓形儀表。
- 大型百分比。

下面只需要一行：

> AI 依目前 SQL 寫法與改善建議進行預估，實際效果仍以後續執行結果為準。

不用要求同仁再輸入第二次 COST。

---

# 十七、網頁畫面大概會長什麼樣？

整個系統只有一個主要 Dashboard。

畫面大致分成：

```text
┌─────────────────────────────┐
│ SQLCheck 2.0                │
├─────────────────────────────┤
│ 申請單號                    │
│ COST                        │
│ SQL                         │
│ 上傳附件                    │
│ [開始檢核]                  │
├─────────────────────────────┤
│ 中心規範 │ COST │ 改善優先指數 │
│ 改善建議 │ 建議寫法           │
├─────────────────────────────┤
│ 中心規則比對                │
├─────────────────────────────┤
│ 智慧改善建議                │
├─────────────────────────────┤
│ 預估改善效果 45%            │
├─────────────────────────────┤
│ 原始 SQL  │ 建議寫法        │
├─────────────────────────────┤
│ 列印 / 存成 PDF             │
└─────────────────────────────┘
```

---

# 十八、畫面設計風格

整體設計方向是：

> **Apple 式淺色、簡約、現代、留白多、資訊圖卡化。**

我們不做：

- 黑底駭客風。
- 一大堆技術數字。
- 很複雜的 DBA 報表。
- 太多動畫。
- 太多不同顏色。

主要用 Card 方式呈現。

顏色大致代表：

- 綠色：符合、目前良好。
- 黃色：提醒、建議改善。
- 紅色：不符合或優先改善。
- 藍色：一般資訊、COST。
- 紫色：AI 智慧改善建議。
- 灰色：次要資訊。

---

# 十九、為什麼畫面不用太多技術術語？

因為主要使用者不是 DBA。

所以畫面會盡量使用：

- 改善
- 建議
- 提醒
- 可以留意
- 可評估
- 建議寫法

避免：

- Semantic Security
- Candidate SQL
- Execution Plan
- Risk Score
- Agent
- Model Context

必要的 SQL 專有詞，例如：

- WHERE
- OR
- LIKE
- TRUNC
- COST
- Parallel Hint

才會保留。

---

# 二十、資料會不會送上網？

不會。

AI 是跑在同一台 Windows 11 工作站上的 Ollama。

流程是：

```text
SQLCheck Docker
      ↓
Windows Ollama
      ↓
Gemma 4 31B
```

不需要呼叫雲端 AI API。

這也是我們採用地端 AI 的主要原因之一。

---

# 二十一、AI 會看到所有 SQL 內容嗎？

系統會盡量先把可能是個資或敏感內容的固定值遮掉。

例如：

```sql
WHERE IDN_BAN = 'A123456789'
```

送給 AI 前可以變成：

```sql
WHERE IDN_BAN = :STR_001
```

AI 還是看得懂 SQL 的結構，但不用知道真正的值。

---

# 二十二、為什麼不讓 AI 直接判斷所有事情？

因為 AI 的回答有可能每次略有不同。

所以重要的事情：

- COST 是否超過門檻。
- 有沒有 WHERE。
- 有沒有 Parallel Hint。
- 是否符合規則。
- 改善優先指數。

都由固定程式判斷。

AI 只做：

> **人類比較擅長閱讀、解釋、提出建議的部分。**

這樣系統會比較穩定。

---

# 二十三、為什麼不做很複雜的 AI Agent？

因為沒有必要。

我們不需要 AI：

- 自己跑 SQL。
- 自己登入 Oracle。
- 自己操作資料庫。
- 自己一直呼叫工具。
- 自己改系統設定。

所以 AI 的流程很簡單：

```text
SQLCheck 整理資料
       ↓
交給 Gemma
       ↓
Gemma 回傳固定格式建議
       ↓
SQLCheck 顯示結果
```

越簡單越好維護。

---

# 二十四、最後 PDF 怎麼產生？

不另外建立複雜的 PDF 系統。

直接使用瀏覽器：

> **列印 → 另存為 PDF**

列印版可以包含：

- 申請單號。
- 原始 COST。
- 中心規範判定。
- 改善優先指數。
- 規則檢核結果。
- 智慧改善建議。
- 預估改善效果。
- 原始 SQL。
- 建議寫法。

之後再依原本流程把 PDF 上傳 SPM。

---

# 二十五、這套系統最大的設計特色

如果要快速記住，我認為只有五件事。

## 1. 不連 Oracle

SQLCheck 不碰中心資料庫。

---

## 2. 不存資料

沒有應用資料庫，也不保存歷史案件。

---

## 3. 規則負責判定

固定規則由程式判斷，不交給 AI。

---

## 4. AI 負責解釋與建議

Gemma 4 31B 幫同仁把技術內容變成容易理解的改善方向。

---

## 5. 一次輸入、一次完成

同仁只要輸入：

> 申請單號 + SQL + COST

就可以完成整次檢核。

---

# 二十六、最簡單的架構圖

```text
┌─────────────────────────────┐
│        使用者瀏覽器         │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│    SQLCheck Docker Container │
│                             │
│  網頁畫面                   │
│  SQL / 附件解析             │
│  規則檢核                   │
│  改善優先指數               │
│  AI 呼叫                    │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ Windows 11 Ollama           │
│ Gemma 4 31B                 │
│ RTX 4090                    │
└─────────────────────────────┘
```

沒有：

```text
Oracle 連線
資料庫
第二個 Container
雲端 AI
```

---

# 二十七、如果要用一句話說明這套系統的設計哲學

> **讓規則負責「判斷」，讓 AI 負責「說明與建議」，讓整套系統保持簡單、地端、無資料庫、一次完成。**

這就是 SQLCheck 2.0 現在最核心的設計邏輯。
