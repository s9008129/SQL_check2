# SQLCheck 2.0 修正後 E2E 測試報告（2026-09-16）

測試對象：開發機（10.97.15.54），本機 FastAPI 後端 + 假 Ollama（`scripts/fake_ollama.py`）
測試素材：使用者昨天實際測試用的 6 份真實檔案
（地價稅.sql、房屋稅-正期免稅數.sql、房屋稅-隨課開徵數.sql、
LND_公益出租人核准案件.docx、LND_台糖馬稠後產業園區土地課稅情形.docx、
LND_自用住宅用地核准件數.docx）

---

## 一句話總結

昨天回報的「AI 一律不給建議寫法」「outer join 被死板判不符合」兩個問題，根因都已在開發機
用這 6 份真實檔案重現、修正、並重新測試通過。**但有一項無法在開發機驗證**：真正的
Gemma4-31B 模型看到新的守門條件與新的 system prompt 之後，是否真的會給出正確的建議寫法，
這需要正式主機的真實 Ollama 才能驗證，開發機只能用假資料模擬「管線有沒有接對」，無法模擬
「模型會不會答對」。這一項留在本報告最後一節，交給您或正式主機測試判斷。

---

## 修好的兩個根因（皆已重現、修正、重測）

### 根因 1：AI 守門條件把幾乎所有真實查詢都擋住了，不是模型保守

在開發機直接呼叫系統自己的規則判斷程式（不需要真實 Ollama）跑這 6 份檔案，結果 6 份**全部**
被系統自己判定「不允許 AI 給建議寫法」：3 份因為有 `GROUP BY` 彙總、3 份因為有 `LEFT JOIN`。
系統的設定檔把這兩種寫法（還有 `DISTINCT`）都列為「禁止 AI 改寫」，而這兩種寫法幾乎出現在
所有真實的稅務報表查詢裡——等於 AI 從頭到尾沒有機會被要求改寫，畫面上看到的「本次先提供
改善方向」其實是系統自己擋下來的，不是 Gemma4 判斷過後才保守拒絕。

**已修正**：放寬這個守門清單，改成用另一套「改寫前後結構是否一致」的複核機制把關（確認
JOIN 種類與順序、GROUP BY、DISTINCT、彙總函數、欄位數改寫前後完全相同），取代原本「乾脆
整類禁止」的做法。

**重新測試結果**：在開發機啟動本機後端 + 假 Ollama，重新送出這 6 份檔案，程式的 log 明確
顯示全部 6 份檔案的「是否允許改寫」都變成「允許」（附錄有 log 截圖文字）。

### 根因 2：附件裡的查詢條件被系統自己弄丟了

其中「LND_台糖馬稠後產業園區土地課稅情形.docx」這份檔案，昨天測試被系統判定「不符合中心
規範（缺少查詢條件）」。實際檢查後發現：這份 Word 文件裡，SQL 的查詢條件（WHERE）前面
剛好有連續 4 個空行，而系統擷取 SQL 內容的程式邏輯，遇到連續多個空行時會把後面的內容整段
捨棄——查詢條件因此在「讀取附件」這一步就不見了，不是規則判斷錯誤，也跟 LEFT JOIN 完全
無關。

**已修正**：調整讀取邏輯，讓連續多個空行也能正確視為同一段落，不再誤刪後面的內容。

**重新測試結果**：這份檔案重新測試後，查詢條件正確保留，系統判定改為「符合中心規範」。

---

## 順便處理的相關問題

- **貼近實務的查詢條件判斷**：您提出「有些 SQL 用 JOIN 條件表達限制意圖，不應該因為沒寫
  WHERE 關鍵字就死板判不符合」——已改為「看限制條件證據」而非只看關鍵字，並在畫面說明文字
  上區分「JOIN 條件確實限縮了結果」與「JOIN 條件只限縮了其中一張表，主表可能仍需留意」兩種
  情況，不講超出系統能確認範圍的話。這個判定的寬嚴可以之後在設定檔調整。
- **AI 完全看不到查詢條件的實際內容，也是原因之一**：例如 `LIKE '114%'` 這種常見的年度
  前綴寫法，原本系統會把所有文字內容都遮蔽成看不出內容的代號，AI 因此無法判斷這是「年度」
  還是別的東西，也就給不出「改成日期範圍」這類具體建議。已調整為「4 個字元以內的英數代號
  或萬用字元樣式」維持可讀，個資（姓名、身分證、地址、完整日期）仍然全部遮蔽。
- **正式主機的紀錄檔看不出「為什麼沒給建議寫法」**：程式原本這類決策紀錄的等級設得太低，
  正式主機的紀錄檔其實從未寫入這些關鍵資訊。已提高紀錄等級，日後若同仁遇到類似狀況，
  可以直接查看正式主機紀錄檔看出具體原因，不需要再靠猜測。
- **AI 回覆被截斷的問題**：因為建議寫法變多之後，AI 允許回覆的長度上限偏低，可能把回覆
  「攔腰截斷」變成無效內容，導致又跳回「暫時無法使用」。已提高上限，並讓系統在偵測到
  「回覆被截斷」時直接明確降級、不做無意義的重試等待。

---

## 新增：去識別化 SQL 蒐集檔（您的第 3 項需求）

已新增一個簡易的 JSON 純文字儲存機制，把每一次「有勾選 AI 建議」的檢核結果，去除所有可能
的個資後（申請單號、姓名、身分證、日期、原始查詢文字內容一律不保留原文），存到正式主機的
`D:\dev\SQL_check2\data\sql_archive\` 資料夾，每月一個檔案。目的是方便日後把大量 SQL
交給 AI 或人工做統計分析。

**已測試確認**：用 6 份真實檔案跑過一次完整流程後，直接在產生的檔案裡搜尋原始個資字串
（身分證字號、地址代碼、日期），確認完全找不到，確認去識別化有效。使用細節（如何停用、
如何讀取分析）已寫進 `deploy/README-deploy.md`「去識別化 SQL 蒐集檔」一節。

---

## 這次「沒辦法」在開發機驗證、需要您協助的部分

**開發機沒有 Docker 也沒有真的 Ollama**，這次測試用的是一個「假的」Ollama（固定回覆同一份
假資料，不會真的理解 SQL 內容）。這足以驗證：

- 系統現在「有沒有把 AI 叫起來、有沒有正確傳資料過去、傳回來的結果有沒有正確處理」——
  這部分已經用假資料完整測過，log 證實運作正常。

但**無法**驗證：

- 真正的 Gemma4-31B 模型，看到新的 system prompt 指示之後，**是否真的會針對 LEFT JOIN／
  GROUP BY 這類查詢給出正確、通過複核的建議寫法**。這需要正式主機的真實模型才能測試。

**建議的驗證方式**（正式主機重新部署後）：
1. 用昨天同樣的 6 份檔案（或任選一份含 LEFT JOIN／GROUP BY 的查詢）重新測試。
2. 觀察畫面上「建議寫法」是否出現具體改寫內容（而不是固定的那句話）。
3. 若某一份仍然沒有給出改寫，執行 `docker compose logs sqlcheck`，搜尋 `ai_service` 關鍵字，
   log 會明確顯示是「模型自己判斷不改」還是「改寫沒通過系統的安全複核」，以及具體原因——
   這是這次新增的功能，之前的版本查不出這個區別。
4. 若複核經常擋下模型的改寫，且複核擋下的理由看起來過於嚴格，這代表 system prompt 或複核
   規則可能需要再微調——屬於後續優化，不影響本次已修正的兩個根因。

---

## 附錄：技術細節（供後續除錯參考）

### 根因 1 的直接證據（開發機重現，未接真實 Ollama）

放寬前，用系統自己的判斷邏輯直接跑 6 份檔案，`candidate_allowed`（是否允許 AI 給建議寫法）
全數為 `False`：

| 檔案 | 觸發旗標 | 放寬前 | 放寬後 |
|---|---|---|---|
| 地價稅.sql | `group_by_aggregate` | False | True |
| 房屋稅-正期免稅數.sql | `group_by_aggregate` | False | True |
| 房屋稅-隨課開徵數.sql | `group_by_aggregate` | False | True |
| LND_公益出租人核准案件.docx | `outer_join` | False | True |
| LND_台糖馬稠後產業園區土地課稅情形.docx | `distinct`, `outer_join` | False | True |
| LND_自用住宅用地核准件數.docx | `outer_join` | False | True |

修正後的 log（本機 E2E 測試，`app.services.ai_service` logger，INFO 等級）：

```
gate candidate=True estimate=True decline_code=None stmt_type=SELECT flags=['group_by_aggregate'] where_kind=None
gate candidate=True estimate=True decline_code=None stmt_type=SELECT flags=['outer_join'] where_kind=None
gate candidate=True estimate=True decline_code=None stmt_type=SELECT flags=['distinct', 'outer_join'] where_kind=None
```

由於本機接的是假 Ollama（固定回覆一段引用不存在資料表的假建議），系統的安全複核正確地
把這個假建議擋下（log：`revalidation rejected: 建議寫法引用的資料表與原始查詢不同`），
這是複核機制正常運作的證據，不是缺陷——真實 Gemma4 會依照實際查詢的資料表回答，不會有
這個問題。

### 根因 2 的直接證據

修正前後對「LND_台糖馬稠後產業園區土地課稅情形.docx」重新跑規則判斷：

| | 修正前 | 修正後 |
|---|---|---|
| 中心規範 | 不符合（BLOCK） | 符合（PASS） |
| WHERE 查詢條件 | 缺少 WHERE 條件 | 已設定／已有限制查詢條件 |
| 擷取出的 SQL 是否含 WHERE | 否（被截斷） | 是 |

### 去識別化蒐集檔驗證方式

對產生的 `sql_archive-2026-09.jsonl` 檔案內容做關鍵字搜尋，確認以下原始值均搜尋不到：
`115051301`（地址代碼）、`1070101`／`1071231`（原始日期）、`王小明`／`A123456789`（測試用
個資樣式）；確認短代碼 `'07%'`（LIKE 年度前綴樣式）與 `:STR_00N` 遮罩代號均正確出現在
去識別化後的內容中。

### 測試涵蓋範圍

- 後端單元測試：194 → 269 項，全數通過；`ruff check` 全過。
- 前端單元測試：52 項全數通過；`npm run build` 成功。
- 本機整合測試（假 Ollama + 真實後端）：6 份真實檔案的 `/api/extract-sql` →
  `/api/analyze`（含 AI）完整跑過，並額外測試「AI 回覆被截斷」情境。
- **未涵蓋**：真實 Gemma4-31B 模型輸出品質、正式主機 `./data` 資料夾實際可寫入性、多人
  同時使用情境——皆需正式主機驗證。
