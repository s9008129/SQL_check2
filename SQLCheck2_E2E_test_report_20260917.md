# SQLCheck 2.0 正式主機真實 Gemma4 探測報告（2026-09-17）

測試對象：正式主機 https://10.97.15.58（已部署 2026-09-16 修正版），真實 Gemma4-31B。
測試素材：使用者的 6 份真實檔案＋5 個刻意含可改寫問題的合成 SQL，共 11 案，每案 41–79 秒。

## 一句話總結

昨天修的守門放寬**已生效**：模型在真的有寫法缺陷的案例會給出改寫並通過安全複核。
6 份真實檔案仍沒有建議寫法，是因為**它們本來就寫得很好**，模型正確判定「無可改之處」。
您看到的「為避免改變原本查詢內容…」是畫面用同一句話呈現三種不同情況造成的誤解，本次已改為三種文案。

## 11 案結果

| 案例 | 規則 | 模型結果 | 判斷 |
|---|---|---|---|
| 地價稅.sql | 符合＋重要表提醒 | 不改寫：條件已分開比對、串接僅顯示用 | 正確（無可改） |
| 房屋稅-正期免稅數.sql | 符合＋重要表提醒 | 不改寫：皆等號比對、無缺陷 | 正確 |
| 房屋稅-隨課開徵數.sql | 符合＋重要表提醒 | 建議 LIKE '07%' 改範圍，不確定格式故不改 | 假改善（Oracle 本就範圍掃描）→ prompt 已修 |
| LND_公益出租人 | 全符合 | 不改寫：無缺陷 | 正確 |
| LND_台糖馬稠後 | 全符合 | 不改寫：建議評估 DISTINCT／欄位型別 | 正確（建議偏空泛 → prompt 已修） |
| LND_自用住宅 | 全符合 | 建議 LIKE '114%' 改範圍，不確定格式故不改 | 假改善 → prompt 已修 |
| S1 TRUNC(欄位)=日期 | R005 提醒 | **給出改寫**，範圍比對，複核通過，預估 40% | 正確 |
| S2 前置萬用字元＋OR | R004/R006 提醒 | 不改寫（無等價寫法） | 正確 |
| S3 TO_CHAR＋NVL | R005×2 | **給出改寫**，兩處函數皆移除，複核通過，預估 40% | 正確 |
| S4 欄位串接='551' | 重要表提醒 | 不改寫：不知 2+1 或 1+2 碼切分 | 正確（需業務假設） |
| S5 LIKE '114%' | 全符合 | 同 LND_自用住宅 | 假改善 → prompt 已修 |

## 本次修正

1. 模型必須回傳 `rewrite_outcome`（provided／not_needed／advice_only），伺服器再標 gated／rejected。
2. 前端三種情況三種呈現：「無需改寫」（綠色，正面）、「僅提供方向」（黃色，附原因與示範片段）、「本次不提供」（系統守門／複核擋下，維持 PRD 固定句）。預估幅度在「無需改寫」時顯示「目前寫法良好，未發現明顯改善空間」。
3. prompt：涉及 SQL 文字的建議必須附 example 片段（需要假設就寫進片段，例如「若 TAX_CD 為 2 碼：…」）；後置萬用字元 LIKE 不再當成改善項目；沒東西可改時不要湊空泛建議。
4. 蒐集檔記錄 `rewrite_outcome`，方便日後統計三種情況的比例。

## 驗證

後端 277 項測試、前端 57 項測試、`npm run build` 全過。真實 Gemma4 對新 prompt 的反應需正式主機重新部署後再驗：預期 6 份檔案顯示「無需改寫」，含 TRUNC 的 SQL 仍給改寫。
