# SQLCheck 效能改善建議助手第二次 DBA 稽核報告（R2，2026-09-19）

> 角色：Oracle DBA + `sqlcheck-oracle-review` 治理稽核
> 範圍：`/Users/hsiaojohnny/Downloads/MAC-G4-01.pdf` ~ `MAC-G4-05.pdf` 第二版（2026-09-19 16:22~16:26，皆為 2 頁）
> 基準：第一次稽核報告 `20260919_audit_report.md`（H1、H2、M1~M5），`skills/sqlcheck-oracle-review` 全套，`backend/app/knowledge/pattern_catalog.yaml`，`backend/app/prompts/sql_review_zh_tw.txt`
> 方法：R2 全文萃取後逐字比對 R1 缺失是否關閉，並掃描新違規；不臆測原 SQL，只審查報告字面

## 驗收總表

| R1 問題 | R2 狀態 | 說明 |
|---|---|---|
| H1 G4-03 具體 `LIKE '明%'` 非等價示意 | 已關閉 | 具體 SQL 已移除，改抽象描述 |
| H2 G4-04 內文具體 NVL 展開與日期區間 | 已關閉 | 具體 SQL 與自創年份已移除 |
| M1 G4-01 三種說法自相矛盾 | 已關閉 | 3 頁併為 2 頁，統一為 1 項建議對 1 項提醒 |
| M2 INFORMATIONAL 被計入寫法項 | 已關閉 | 改稱 1 項建議，不再說寫法 |
| M3 G4-03 計數 1 對 2 | 已關閉但引入新問題 | 計數改為 2 對 2，但 OR 卡新增 UNION ALL 風險（N1） |
| M4 自創日期字面 | 已關閉 | 見 H2 |
| M5 G4-05 無提醒對 1 提醒 | 已關閉 | 頭尾統一為 1 項需確認 |
| N1 G4-03 新增 UNION ALL 建議 | 新開，中高 | 見下 |
| N2 G4-05 舉例日期或狀態欄位 | 新開，低 | 見下 |
| 殘留 R-G4-02 兩卡內文重複 | 殘留，低 | 見下 |
| 殘留 R-G4-04 2 卡對 1 提醒 | 殘留，低 | 可解釋，文案可再修 |

## 逐案驗收

### G4-01：通過，改善確認

- R2 事實：Page1 先看結論符合中心規範另有 1 項建議，AI 1 項確認重要資料表查詢範圍，Page2 本次檢核結果 1 項提醒 WIIT001。無第三頁原寫法頁。
- 改善：M1 的目前未發現需要調整對有改善方向矛盾消失，M2 的寫法計數改為建議計數，頭尾一致。
- DBA 觀點：重要表提醒維持提醒等級，未升級為不符合，合規與優化分家正確。`AI 建議僅供參考，採用前請先測試` 有保留。
- 證據：R2 G4-01 Page1、Page2；對照 R1 G4-01 Page2。

### G4-02：通過，寫得更好，殘留一個小冗餘

- R2 事實：兩卡皆為抽象方向。確認查詢範圍與評估日期比對寫法內文同為目前條件先用 TRUNC 處理欄位再比對。若確認是日期欄位，可評估改用日期範圍；調整前請先確認欄位型態與比對值是否包含時間。Page2 規則為 `TRUNC(A.TXN_DATE)` 可評估改成直接比對欄位值的寫法。
- 改善：R1 的大於等於該日 00:00:00 且小於隔日具體描述已拿掉，通常能讓資料庫有更多機會等效能句也拿掉，更保守。TRUNC 維持 advice_only 不給可貼 SQL，符合 `TRUNC_EQ_TO_RANGE` 與 2026-09-18 停止推導。
- 殘留 R-G4-02：兩卡標題不同但內文完全相同，疑似重要表卡與函數卡共用文案。建議合併或區分：重要表卡寫確認日期範圍與資料量，函數卡寫確認 DATE 型態與綁定值時間成分。
- 證據：R2 G4-02 Page1 兩卡內文；`advice-only-patterns.md` 同項。

### G4-03：計數修好，H1 關閉，但 N1 必修

- R2 事實：Page1 AI 2 項。評估比對範圍需確認：目前使用前置萬用字元。若業務需求允許縮小比對範圍，可評估其他比對方式；調整前請先確認實際比對需求。確認 OR 條件需求提醒：查詢條件中使用 OR 連接不同欄位，請確認此邏輯是否符合業務需求，或可評估是否能拆分為 UNION ALL 以提升查詢效率。Page2 LIKE `%明` 可評估是否有更精確的比對方式，OR 共 1 處可評估是否能簡化。
- 改善：H1 的 `LIKE '明%'` 可貼 SQL 已整段刪除，無 `明%`、`2026`、`IS NULL` 字串，符合 `LEADING_WILDCARD_LIKE` 沒有等價改寫。M3 的 1 對 2 改為 2 對 2。
- 新開 N1 中高：或可評估是否能拆分為 UNION ALL 以提升查詢效率違反 `OR_CROSS_COLUMN_TO_UNION_ALL`。若兩分支可同時成立，UNION ALL 會複製重複列，UNION 則改變去重語意與成本，SQL 文字看不出是否重疊。R2 只給方向未給重疊警告，且以提升查詢效率作確定性效能宣稱，比 prompt 允許的通常有更多機會更絕對。雖有 AI 建議僅供參考兜底，仍不符合 advice 必須點出若不確認會發生什麼。
- 應改為：或可評估是否能拆分，但拆成 UNION ALL 前請先確認兩條件是否可能同時成立；若會重疊，直接拆會複製重複列，改 UNION 又改變去重語意，需業務確認後再由人工改寫。並把以提升查詢效率改為保守句。
- 證據：R2 G4-03 Page1 第二卡全文；禁語掃描本卡命中以提升查詢效率；`pattern_catalog.yaml` 同項 prohibited_claims。

### G4-04：通過，H2 關閉典範

- R2 事實：調整日期比對寫法需確認：目前條件先用 TO_CHAR 轉成文字再比對。可評估改用原欄位型態直接比對；調整前請先確認欄位型態與實際比對需求。調整空值處理寫法需確認：目前條件用 NVL 處理空值。可評估改成分開判斷欄位值與空值；調整前請先確認欄位型態與原本的空值規則。Page2 維持 `TO_CHAR(A.APPR_DATE,'YYYY')`、`NVL(A.CANCEL_FLAG,'N')` 可評估改成直接比對。
- 改善：R1 的 2026-01-01 區間與 `(A.CANCEL_FLAG = 'N' OR A.CANCEL_FLAG IS NULL)` 具體式全部刪除，無自創字面，無 CHAR 陷阱可貼 SQL，符合 prompt 改寫範例 b 的 example 留空。這是本次改得最乾淨的一件。
- 殘留：頭寫 2 項 SQL 改善建議，尾寫 1 項不符合 1 項提醒，數字 2 對 1。實為 R005 一條規則含兩個方向，可解釋，但建議尾加註含 2 個方向或拆 R005 顯示，免得下次又被挑。
- DBA 觀點：COST 125000 判不符合正確，85 分優先改善符合 block_floor 80，TO_CHAR 與 NVL 皆點出型態前提，正確。
- 證據：R2 G4-04 Page1 兩卡；`advice-only-patterns.md` 的 `NVL_EQ_TO_OR_IS_NULL` CHAR 說明。

### G4-05：通過，M5 關閉，N2 小修

- R2 事實：Page1 有 1 項需要人工確認，中規 1 項需確認，AI 2 項。確認查詢範圍提醒：目前 SQL 沒有主查詢的 WHERE 條件，可能會帶出 LND_CASE 表的所有資料，建議確認是否需要增加日期或狀態等限制條件以縮小範圍。評估移除 DISTINCT 需確認：移除前請先確認 JOIN 後是否仍可能出現重複資料；如果會，就不要移除。Page2 WHERE 限制條件只在 JOIN ON，主查詢未寫 WHERE，JOIN 的 ON 不會縮小主表範圍，請人工確認。
- 改善：R1 的目前無提醒事項對 7 符合 1 提醒矛盾已統一為 1 項需確認。LEFT 未被改 INNER，未自動補 WHERE，DISTINCT 明確給如果會就不要移除，符合 `LEFT_JOIN_TO_INNER_JOIN` 與 `DISTINCT_REMOVAL`。
- 新開 N2 低：建議確認是否需要增加日期或狀態等限制條件直接點名兩個欄位種類。系統看不到 schema，prompt 規定缺業務資訊不自行猜測 WHERE JOIN 欄位。LND_CASE 是否真有日期狀態不得而知，應改為建議確認是否需要增加限制條件以縮小範圍，舉例如確知有日期狀態欄位再由業務指定。
- 證據：R2 G4-05 Page1 第一卡；`rules.yaml` R002 `accept_join_on: false`；prompt 不猜測欄位段。

## 共通檢查 R2

- 禁語掃描：5 份皆無 Full Table Scan、全表掃描、索引失效、已使用索引、未使用索引、改善後 COST、執行計畫顯示、已驗證、已測試、實測、高風險、嚴重問題、錯誤寫法。唯一命中是以提升查詢效率，僅 G4-03 一處，見 N1。
- 具體 SQL 殘留掃描：`明%`、`2026-01-01`、`IS NULL` 在 R2 5 份皆無命中，H1 H2 確實拔乾淨。
- 體積：R1 每份約 2280~3020 字元 3 頁，R2 每份約 1263~1537 字元 2 頁。第三頁建議採用狀態頁整頁刪除是矛盾消失的主因，方向正確。
- 指數：25、55、50、85、40 與 R1 完全一致，名稱由改善優先指數簡化為改善指數。確定性未被 AI 污染，符合 `rules.yaml` improvement_score。但指數組成仍收合，無法驗證 F S C 明細。
- 免責：長版不代表實際效能提升與請勿直接套用被縮為 AI 建議僅供參考，採用前請先測試。尚可接受，但建議加回不代表效能提升幅度八字以對齊 `ACTUAL_RUNTIME_IMPROVEMENT_CLAIM`。

## 下一步

- 必改再測：G4-03 第二卡按上文補重疊警告並改保守句，改完重出一次 PDF 即放行。
- 順手修：G4-02 兩卡去重，G4-05 日期或狀態改抽象，G4-04 尾加註 1 項提醒含 2 方向。
- 測試：新增 UNION ALL 無重疊警告檢測，新增內文具體 SQL 檢測沿用 H1 H2 規則，新增頭尾計數一致性檢測。
- 可追溯：PDF 加原 SQL 去識別化雜湊，Learn 迴圈才接得上。

## 結論

- R1 的兩個高風險與三處自相矛盾皆已修掉，G4-01、G4-02、G4-04、G4-05 可放行，只剩 polish。
- G4-03 修好計數與 LIKE，卻引入 N1，請按上文修一句話後再放行。

---

# 附錄 A：DIFF-TEST-01、02 改寫對照 UX 深究（2026-09-19 加註）

> 目標：AI 提供改寫＋SQL 前後對照，非技術 SQL 撰寫人一目瞭然
> 標的：`DIFF-TEST-01` 同欄位 OR 改 IN（`A.STATUS_CD IN ('A','B')`，20 分），`DIFF-TEST-02` SUBSTR 改 LIKE（`A.MANAGE_CD LIKE '_____551%'`，35 分）
> 兩件皆為 Class A VERIFIED_REWRITE（`OR_SAME_COLUMN_TO_IN`、`SUBSTR_EQ_TO_LIKE`），是全站唯二可自動改寫的型別，故本次出現已確認屬實，不是誤標

## A1. 已確認三字是否適當

- 現況：同一份出現 4 次。Page1 智慧卡徽章已確認，Page2 卡頭建議寫法徽章已確認，全文對照表頭建議寫法（已確認），片段標題建議寫法（已確認）。程式對應 `ImprovementAdvice.tsx` EVIDENCE_META confirmed（綠，title 此建議的 SQL 改寫已通過系統的確定性驗證）、`SqlCompare.tsx` statusText 寫死已確認、`SqlDiffView.tsx` FRAGMENT_LABEL。
- 痛點成立：已確認缺受詞與動作。撰寫人讀到的是誰確認、確認什麼、我該做什麼三問皆無答。tooltip 雖寫確定性驗證，非技術人看不懂。與需確認、提醒並列時，三者看似同級狀態而非行動指引。
- 是否可刪：不可整組刪除。`methodology.md` Present 步驟要求 verified、corrected、unverified、advice-only 視覺與文字必須可區分，否則會重蹈把建議當保證的覆轍。但三字本身可以換，徽章的作用應從系統狀態改為使用者行動。
- 建議（best practice，動詞優先、受詞明確、後果可預期）：
  - 已確認 → 可直接用（副標：系統已比對，查出結果相同）。保留綠色，tooltip 改為系統已用程式比對過，改前改後查出的資料列相同且符合 Oracle 語法，可直接貼上，正式跑前仍請測試。
  - 需確認 → 請先確認再用（副標：需業務條件）。黃色不變。
  - 提醒 → 僅供參考（副標：觀念提醒）。紫色不變。
  - 表頭建議寫法（已確認）→ 改後寫法（可直接用）。原寫法保留。
  - 若堅持保留已確認一詞，最低限度改為已確認結果相同，五字含受詞與結果，仍不如可直接用直覺。
- 為何這樣改：Nielsen 啟發式之使用貼近真實世界的語言、GOV.UK plain language 之動詞開頭、WCAG 之標籤即行動。測試方法：找 3 位非技術同仁各讀 30 秒，能說出這段能不能直接貼上即達標。

## A2. 原寫法、建議寫法、寫法對照是否重複

- 現況：Page2 同時有全文逐行對照（7 行，黃底標修改處，未改的 SELECT、FROM、AND 原樣重印）與寫法對照片段（只印被改的 WHERE 條件一行）。兩案皆只有 1 個條件被改，全文區 5 行未變，片段區與全文區的建議寫法（已確認）同名，確實三重。
- 你的想法基本正確：對非技術人，預設只留寫法對照片段是對的。理由：注意力只應放在被改的那一行；全文重印增加捲動與列印頁數（本次 3 頁多半由此而來）；行號因換行錯位（01 案第 5 行 WHERE 被切成兩行，02 案第 6 行同樣）反而干擾；片段已有黃底詞級 diff，足夠一目瞭然。
- 但不建議物理刪除全文：全文是可貼上線的唯一完整成品，也是稽核可追溯的證據。刪掉會逼使用者自行拼接片段回整段，出錯率更高。
- 建議（漸進揭露）：預設顯示寫法對照片段為主，卡頭改為重點改寫（可直接用）。全文逐行對照收合為可展開的完整 SQL（一鍵複製），預設收起，列印僅印片段。如此非技術人只看一屏，技術人與稽核仍拿得到全文。
- 命名收斂：建議寫法大卡標題與片段標題同名是混亂主因。改為大卡：改寫對照；片段：重點改寫；全文：完整 SQL。這樣三名各指一物，不再互指。

## A3. 本次兩案的 DBA 核可

- DIFF-TEST-01：`A.STATUS_CD = 'A' OR A.STATUS_CD = 'B'` 同欄位等值轉 `IN ('A','B')`，等價無疑，ORACLE_IN_LIST 上限內，核可。Page3 OR 仍列提醒可評估是否能簡化屬家族訊號殘留，既然已給出 VERIFIED 改寫，建議該提醒自動收斂或註明本案已改寫，免得同頁又叫人簡化。
- DIFF-TEST-02：`SUBSTR(A.MANAGE_CD,6,3)='551'` 轉 `LIKE '_____551%'` 為 canonical LIKE 唯一授權形，無前綴範圍形，核可。Page1 可改用對應的 LIKE 寫法讓結構更簡潔一句，簡潔非重點，重點是結果相同，建議改為與 A1 同一句系統已比對結果相同。
- 兩案皆未出現索引、執行計畫、COST 宣稱，`AI 建議僅供參考，採用前請先測試` 有保留，合規。

## A4. 待辦（併入第二版結論）

- 必改：A1 徽章三名改行動式（可直接用、請先確認再用、僅供參考），tooltip 同步白話；A2 全文預設收合，片段為主，卡名互斥。
- 順手：DIFF-01 的 OR 提醒在已給 VERIFIED 時收斂，DIFF-02 的更簡潔改結果相同。
- 測試：5 秒理解性測試（能不能貼）、片段與全文一致性測試、列印頁數測試。
