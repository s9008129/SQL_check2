# SQLCheck AI 系統架構與設計簡易說明

> 本文件提供給非技術人員閱讀，說明系統要做什麼、怎麼運作，以及目前已確定的產品邊界。  
> 更新日期：2026-09-20。

## 1. SQLCheck AI 是什麼

SQLCheck AI 是地方稅自撰 SQL 的事前檢核與改善輔助工具。

同仁輸入申請單號、SQL 與 COST 後，系統會先依既有規則檢查，再整理成容易閱讀的結果；AI 只負責白話說明與改善方向，不負責決定「符合／不符合中心規範」。

系統目前使用的正式名稱為：

> **改善指數**

它表示「這支 SQL 有多少地方值得優先檢視」，不是違規分數，也不是 Oracle 實際效能分數。

## 2. 系統會做什麼

主要功能包括：

- 檢查 COST 是否低於中心門檻。
- 檢查 WHERE、Parallel Hint 等規範。
- 提醒 LIKE 前置萬用字元、條件欄位函數、OR 與重要資料表等寫法。
- 計算 0～100 的改善指數。
- 由 AI 用臺灣繁體中文整理改善建議。
- 在系統能證明結果不變時，顯示「系統可確認」的改寫對照。
- 如果需要資料型態、業務條件或資料庫證據才能判斷，就只提供方向，不硬改 SQL。
- 可列印或另存 PDF，配合原有簽核流程使用。

## 3. 系統不會做什麼

SQLCheck AI 不會：

- 連線財政資訊中心 Oracle。
- 執行使用者輸入的 SQL。
- 讀取正式資料。
- 自動修改資料或建立 Index。
- 取得 Execution Plan、Index Metadata、Statistics 或實際執行時間。
- 把 AI 的說法當成合規判定。
- 把看不到的資料庫行為說成已驗證事實。

因此，像「一定走索引」「一定沒有 Full Table Scan」「改寫後會快幾 %」都不是 SQLCheck 可以自行宣稱的事。

## 4. 操作流程

```text
同仁輸入申請單號、SQL、COST
            ↓
      SQL / 附件解析
            ↓
       規則引擎檢核
            ↓
        計算改善指數
            ↓
   系統推導可確認的改寫
            ↓
   將必要資訊去識別化
            ↓
      AI 整理白話建議
            ↓
    伺服器再次安全檢查
            ↓
      網頁顯示／列印
```

規則引擎的結果不會因 AI 回覆不同而改變。

## 5. 改善指數

目前改善指數沿用既有演算法與權重，已於 2026-09-20 正式定案：

| 分數 | 顯示 |
|---:|---|
| 0～59 | 目前良好 |
| 60～79 | 建議改善 |
| 80～100 | 優先改善 |

分數由確定性資訊計算，包括規則命中、SQL 結構與 COST 接近門檻的程度；AI 不參與打分。

即使 SQL 符合中心規範，也可能因多個提醒而有較高改善指數。這表示「值得優先檢視」，不代表違規。

## 6. 重要資料表

重要資料表目前只有「提醒」用途。

例如命中設定中的重要資料表時，R007 會提醒同仁確認查詢範圍與必要性。

2026-09-20 已確認實務上沒有「重要資料表禁止操作矩陣」，因此 R008 已正式停用，不再視為待補需求。

## 7. AI 與去識別化

規則引擎先使用原始 SQL 做本機判斷；送往雲端 AI 前，系統會再做資料最小化。

目前採取的是平衡型做法：

- 中文姓名、地址、Email、電話等長字串會被遮罩。
- DATE / TIMESTAMP 的實際值會被遮罩，但保留必要型態資訊。
- 長數字識別值會被遮罩。
- 一般 SQL 註解會移除，避免把承辦備註或個資送到雲端。
- Oracle optimizer hint 會保留，因為它屬於 SQL 結構的一部分。
- 短 ASCII 業務碼與必要 table / column 結構目前保留，以維持判讀能力。

這項 Balanced privacy policy 已於 2026-09-20 定案，暫不另做 Strict Mode。

## 8. SQL Archive

系統可以把去識別化後的 SQL 與檢核結果寫入 `data/sql_archive/`，供後續離線統計與改善參考。

Archive 不保存申請單號、原始 literal、附件檔名或使用者資訊。

2026-09-20 已決定 retention 維持現況，不新增自動刪除機制。這是維運資料，不會自動提交到 GitHub。

## 9. 簡易架構

正式環境：

```text
使用者瀏覽器
    ↓ HTTPS
SQLCheck App（Docker）
    ├─ React 網頁
    ├─ FastAPI
    ├─ SQL Parser
    ├─ 規則引擎
    ├─ 改善指數
    ├─ Rewrite 驗證
    └─ AI Service
           ↓
Windows 原生 Ollama
           ↓
Gemma 4 31B
```

正式環境只有 SQLCheck App 放在 Docker；Ollama / Gemma 執行在 Windows Host。

## 10. 測試與驗收

一般程式修改由 GitHub CI 自動執行單元測試與 lint。

真正呼叫 Ollama Cloud 的 Live Cloud E2E 不會每次 PR 都自動跑，而是需要時在 GitHub Actions 手動啟動。這樣可以避免每個小修改都消耗雲端 API，同時保留重大 AI 變更時可重複驗收的能力。

簡單說：

> 平常改程式跑一般 CI；只有改到 AI、Prompt、遮罩、安全守門或模型設定時，再按一次 Live Cloud E2E 做「真人版體檢」。

## 11. 已結案的治理決策

截至 2026-09-20：

- Cloud privacy：維持目前 Balanced policy，結案。
- SQL Archive retention：維持現況，結案。
- R008 禁止操作矩陣：實務無禁止，保留 R007 提醒，R008 停用，結案。
- 改善指數：名稱已確認為「改善指數」，既有權重與演算法正式定案，結案。
- Ollama 11434 Firewall hardening：由專案 owner 人工處理，不再列為本專案待辦。
- Execution Plan：暫緩，不是目前開發範圍。

## 12. 文件怎麼看

目前哪些文件仍是現行文件、哪些只是歷史報告，統一以：

- `docs/document-status.md`

為準。

舊 PRD、舊 E2E report、audit report、Handoff 與 Prototype 仍保留作為歷史紀錄，但不應再用來判斷目前系統行為。
