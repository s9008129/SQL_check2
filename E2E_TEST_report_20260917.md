# SQLCheck 2.0 正式主機 E2E 測試報告（2026-09-17，部署後複測）

測試對象：https://10.97.15.58（使用者於本次對話中重新部署完成，commit `f0bfddf` 建議寫法
三態呈現版本）。測試方式：對正式主機發送真實 API 請求（含真正呼叫 Gemma4-31B），並用
瀏覽器自動化實際操作網頁確認畫面渲染結果。

---

## 一句話總結

**本次更新已解決問題，沒有發現需要修復的異常。** 11 個 API 測試案例與 3 個瀏覽器實測案例
全數通過，「無需改寫」「僅提供方向」「可供參考的建議寫法」三種情況現在會顯示三種不同的
畫面文字，不再全部擠成同一句「為避免改變原本查詢內容…」。

---

## 測試範圍

### 第一部分：API 層測試（11 案，直接呼叫正式主機 `/api/extract-sql`、`/api/analyze`）

沿用 2026-09-16 探測用的 6 份使用者真實檔案，加上 5 個合成案例，逐一送出並記錄完整回應。

| 案例 | 規則結果 | AI 判斷 outcome | 備註 |
|---|---|---|---|
| 地價稅.sql | 符合＋重要表提醒 | `rejected` | 見下方「特別記錄」 |
| 房屋稅-正期免稅數.sql | 符合＋重要表提醒 | `not_needed` | 正確 |
| 房屋稅-隨課開徵數.sql | 符合＋重要表提醒 | `not_needed` | 正確辨識後置萬用字元 LIKE 已足夠有效率 |
| LND_公益出租人核准案件.docx | 全符合 | `not_needed` | 正確，advice 為空陣列 |
| LND_台糖馬稠後產業園區土地課稅情形.docx | 全符合 | `not_needed` | 正確，advice 為空陣列 |
| LND_自用住宅用地核准件數.docx | 全符合 | `not_needed` | 正確辨識 LIKE '114%' 已足夠 |
| S1：`TRUNC(欄位)=日期` | R005 提醒 | `provided` | 給出正確改寫，通過複核，預估 30% |
| S3：`TO_CHAR`＋`NVL` | R005×2 | `provided` | 給出正確改寫，通過複核，預估 40% |
| S4：欄位串接 `='551'` | 重要表提醒 | `advice_only` | 建議卡片內含假設片段（見下方截圖文字） |
| S5：`LIKE '114%'` | 全符合 | `not_needed` | 正確 |
| S6：乾淨簡單查詢 | 全符合 | `not_needed` | 正確，advice 為空陣列 |

### 第二部分：瀏覽器實測（3 案，實際操作 https://10.97.15.58 網頁）

用 Playwright 直接在網頁輸入申請單號、COST、SQL 並點擊「開始檢核」，等待 AI 回應後檢查畫面文字：

1. **S6（乾淨 SQL）→ `not_needed`**：
   - 摘要卡片「建議寫法」顯示 **✓ 無需改寫 / 目前寫法已良好**（綠色）。
   - 「預估改善效果」卡片顯示 **「目前寫法良好，AI 未發現明顯的改善空間。」**（綠色正面文字，不是灰色的「不提供」）。
   - 「SQL 寫法比較」區顯示 **「AI 檢視後認為目前寫法已良好，本次不需要改寫。」**

2. **S1（TRUNC 日期比對）→ `provided`**：
   - 摘要卡片「建議寫法」顯示 **✓ 可供參考 / 原始 SQL 完整保留**。
   - 「SQL 寫法比較」區實際顯示改寫後的完整 SQL：
     `SELECT A.X FROM HOUT120 A WHERE A.TXN_DATE >= TO_DATE('20240101', 'YYYYMMDD') AND A.TXN_DATE < TO_DATE('20240101', 'YYYYMMDD') + 1`
     （佔位符已正確還原成真實日期值，改寫語意正確）。

3. **S4（欄位串接）→ `advice_only`**：
   - 摘要卡片「建議寫法」顯示 **· 僅提供方向**（黃色），說明文字為「由於欄位串接 '551' 的拆分方式（例如 2+1 碼或 1+2 碼）需要根據業務定義確認…」。
   - 「智慧改善建議」卡片的示範片段（`<code>`）正確顯示：
     `若 TAX_CD 為 2 碼且 SUBTAX_CD 為 1 碼：WHERE W.TAX_CD = '55' AND W.SUBTAX_CD = '1'`
     — 完全符合使用者決定的「假設式改寫只給片段範例＋說明假設」。

**結論：三種畫面狀態皆與設計一致，實機渲染正常，沒有發現前端未套用新版程式碼或快取殘留的情況。**

---

## 特別記錄一項值得留意但不算異常的行為（地價稅.sql，outcome=`rejected`）

這是本次測試中唯一一個 outcome 為 `rejected` 的案例，記錄如下供後續分析參考：

- 原始 SQL：`GROUP BY w.tax_cd||w.subtax_cd`（依欄位串接後的結果分組）。
- Gemma4 建議：`GROUP BY w.tax_cd, w.subtax_cd`（改成依兩個獨立欄位分組），理由是避免資料庫在分組時對每筆資料做字串串接運算。
- 系統的結構複核擋下了這個建議，原因是「建議寫法的 GROUP BY 與原始不同」（複核只看分組欄位**數量**是否相同：原本 1 項、建議 2 項，數量不同就直接判定「不同」，不會去判斷兩種分組方式在這個案例中其實會產生等價的分組結果）。
- 使用者看到的畫面文字是：「AI 提出的建議寫法未通過系統安全複核（建議寫法的 GROUP BY 與原始不同），為避免改變原本查詢內容，本次不顯示建議寫法。」——這句話**如實反映了系統的判斷過程**，不是含糊的固定文案，符合本次修正的目標（決策透明化）。

**這不算是本次任務要修的缺陷**，原因：
1. 系統的複核邏輯是刻意設計成保守的——「分組欄位數量是否相同」是一個簡單、可驗證的規則，而「兩種不同的 GROUP BY 寫法在語意上是否等價」需要更深入的語意分析（例如確認欄位串接是否為單射函數），目前系統選擇「不確定就不顯示」，這是安全優先的合理取捨，不是程式錯誤。
2. 使用者仍然可以在「智慧改善建議」卡片看到這個建議與示範片段（`GROUP BY w.tax_cd, w.subtax_cd`），只是不會被包裝成「可以直接複製使用的完整改寫 SQL」。
3. 若日後要放寬複核邏輯讓這類「分組欄位拆分但語意等價」的改寫也能通過，屬於功能增強而非修 bug，需要另外評估拆分後是否在所有情況下都保證分組結果一致（例如欄位長度不固定時可能不成立），建議列入未來優化項目、不建議倉促放寬。

---

## 測試證據總結

- API 層：11/11 案例回應正確，`compliance` 判定皆為 `PASS`，`ai_status` 皆為 `ok`，無逾時、無例外、無 500 錯誤。
- 瀏覽器層：3/3 案例畫面文字與設計規格完全一致，`無需改寫`／`可供參考`／`僅提供方向` 三種標籤與對應說明文字皆正確渲染，改寫後的 SQL 值正確從遮罩佔位符還原。
- 未發現任何前端顯示與後端資料不一致、快取殘留、或例外報錯的情況。

## 結論與下一步建議

本次「AI 一律不給建議寫法」的原始問題已確認徹底解決：模型的判斷本身是正確的（乾淨的 SQL
判定「無需改寫」、有缺陷的 SQL 給出改寫、需要業務假設的情況給出附假設的方向），畫面呈現
也已同步反映這三種差異。

**不需要立即動作**。若後續想進一步優化，可考慮：
1. 觀察 `data/sql_archive/` 累積的 `rewrite_outcome` 分布，統計「rejected」「advice_only」
   佔比，評估是否值得投入更多心力放寬結構複核的判斷精細度（例如上述 GROUP BY 拆分等價性
   判斷）。
2. 若同仁實際使用後，仍有特定情境覺得複核太保守，蒐集具體案例再評估調整幅度，避免無依據
   放寬安全邊界。
