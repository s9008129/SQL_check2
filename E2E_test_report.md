# SQLCheck 2.0 正式主機完整測試報告

測試對象：https://10.97.15.58（正式主機，2026-09-15 剛部署完成）
測試方式：直接對正式主機發送真實請求，包含真正呼叫 Gemma 4 AI 模型（不是假資料）

---

## 一句話總結

**後端（系統的大腦，負責判斷 SQL 對不對、AI 給建議）完全正常，而且是用「真的」AI 模型測試過的；但「網頁畫面」目前打不開。** 網頁打不開的原因已經找到並修好了，只是這個修正還沒有送到正式主機上，需要您重新部署一次才會生效。

---

## 好消息：這些東西都測過了，而且沒問題

我直接對正式主機發送了 9 個測試請求，模擬同仁實際會輸入的各種 SQL，結果全部正確：

| 測試項目 | 結果 | 說明 |
|---|---|---|
| 系統健康檢查 | ✅ 正常 | 系統回報「AI 可以使用」 |
| 一般乾淨的 SQL | ✅ 正常 | 正確判定「符合規範」 |
| 有問題的日期寫法（TRUNC + 重要資料表） | ✅ 正常 | 正確判定「符合規範，但有 2 項提醒」，**並且真的叫 Gemma 4 給出改善建議**，內容用詞正確、沒有出現不該講的話 |
| 沒寫查詢條件（缺少 WHERE） | ✅ 正常 | 正確判定「不符合規範」 |
| 使用了被禁止的加速指令（Parallel Hint） | ✅ 正常 | 正確判定「不符合規範」 |
| COST 超過門檻（15萬） | ✅ 正常 | 正確判定「不符合規範」 |
| 比較複雜的統計查詢 | ✅ 正常 | 正確判定「符合規範」，AI 判斷這種查詢太複雜，**很保守地不亂給建議寫法**，符合原本的設計原則 |
| 一次貼兩段 SQL | ✅ 正常 | 兩段都正確分開判斷 |
| 上傳 SQL 檔案自動辨識 | ✅ 正常 | 檔案內容正確被抓出來 |

**特別要說明的一點**：其中兩項測試是真的呼叫了 Gemma 4 這個 AI 模型，不是用假資料測試。AI 回答的內容我有檢查過，確認：
- 沒有出現不該講的話（例如「全表掃描」「已驗證」「高風險」這類不允許出現的字眼）
- 用詞是正確的台灣繁體中文，沒有跑出大陸用語
- 給的「預估改善幅度」數字合理（一個是 30%，另一個因為查詢太複雜所以正確地沒有給數字）

## AI 回覆需要多久？（重要，供您評估使用體驗）

這次量測到 AI 部分的回覆時間是 **22 秒到 49 秒**之間。同仁使用時，畫面會先立刻顯示規則檢核結果（這部分是秒回的），AI 建議的部分會顯示「AI 分析中」，大約幾十秒後才會出現，這是正常現象，不是當機。如果之後實際使用覺得等待時間太久，屬於可以之後再調整的項目，不是本次測試發現的問題。

---

## 發現的問題：網頁打不開

用瀏覽器實際打開 `https://10.97.15.58` 時，畫面顯示「找不到網頁」，而不是同仁應該看到的申請單號／SQL／COST 輸入畫面。

**這不是「系統壞了」，而是一個很單純的小疏失**：程式在正式主機的資料夾裡去找「網頁畫面檔案」的時候，找錯了資料夾層數（就像要去「隔壁棟」拿東西，結果只走到「隔壁房間」就停下來找，當然找不到）。因為找不到，系統就完全沒有把「顯示網頁」這個功能打開，只剩下後台的功能還在運作——這也是為什麼上面那 9 項後台測試全部正常，但瀏覽器打開卻是空的。

這個問題不會影響任何人已經送出的資料，也不是資料外洩或安全性的問題，純粹是「網頁打不開」。

### 我已經做的處理

1. 已經找到並修好程式裡算錯資料夾層數的地方。
2. 已經另外寫了 3 個自動檢查，專門確認「這個資料夾層數不會再算錯」，並且把後端所有測試（194 項）都重新跑過一次，確認這個修正沒有把其他功能弄壞。
3. 這個修正已經整理成一筆清楚的紀錄，準備推送到程式碼庫。

### 您需要做的事

因為這個修正是改「程式碼」，正式主機上目前跑的是修正前的舊版本，**需要您在正式主機重新部署一次才會生效**：

```powershell
cd D:\dev\SQL_check2
git pull
cd deploy
.\deploy.ps1
```

因為只是改程式碼（沒有改模型設定、防火牆設定），這次重新部署應該會比第一次快很多，主要就是重新打包一次網頁畫面。部署完成後，麻煩用瀏覽器打開 `https://10.97.15.58` 確認畫面正常出現，我這邊也可以在您重新部署後，再幫您跑一次「用瀏覽器實際操作」的完整測試（貼 SQL、看結果、上傳附件、列印預覽），把最後這一塊補齊。

---

## 這次測試「沒有」涵蓋的部分

- **瀏覽器實際畫面操作**：因為上述問題，這次無法用瀏覽器實際點畫面測試（例如按鈕、版面排版、手機瀏覽畫面）。建議重新部署後再測一次。
- **多人同時使用**：這次只測試單一使用者依序送出請求，沒有測試很多人同時使用會不會變慢或出錯。

---

## 附錄：給技術人員的簡短補充

- 根本原因：`backend/app/main.py` 判斷網頁檔案位置時，用 `Path(__file__).resolve().parent / "static"`（只往上一層，等於 `app/static`），但 Dockerfile 實際把前端 build 結果複製到 `backend/static`（往上兩層才對）。本機開發測試時因為走的是另一個候選路徑（`frontend/dist`）而沒發現，只有在正式主機用真正的 Docker 容器結構時才會暴露。
- 修正：改為 `Path(__file__).resolve().parent.parent / "static"`，並抽成獨立函式 `_find_static_dir()`，補上 3 個用假資料夾結構模擬真實容器路徑的單元測試，防止未來再犯同樣的錯。
- 驗證方式：直接對 `https://10.97.15.58/api/*` 發送 9 個真實請求（含 2 個真實 Gemma 4 呼叫），全數通過；後端 `pytest` 194 項全數通過。
- 尚待驗證：修正後的版本需要正式主機重新 `docker compose up -d --build` 生效，目前正式主機上跑的仍是修正前的映像檔。
